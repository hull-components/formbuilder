<input  id="{{input_id}}" type='email' name='{{name}}' class="form-control"
  {{#required}}required{{/required}} 
  data-parsley-trigger="change"
  value="{{value}}" 
  placeholder="{{field_options.placeholder}}"
/>